# 📝 To-Do List GUI App (Python Tkinter)

A colourful and interactive **To-Do List Desktop Application** built using **Python & Tkinter**.

This app allows users to:
- ✔ Add tasks
- ✔ Delete tasks
- ✔ Tick checkboxes to mark tasks as completed
- ✔ Automatically save tasks using JSON
- ✔ Auto-load tasks when the app restarts
- ✔ Windows .exe version supported

---

## 🚀 Features

### 🎨 Beautiful UI
The application includes a clean, modern, and colourful user interface using Tkinter widgets and custom colours.

### ✔ Task Checkboxes
Each task is displayed with a checkbox so users can mark tasks as completed.

### 💾 Auto Save
All tasks are automatically saved in `tasks.json`, even when the app is closed.

### 🔁 Auto Load
Whenever the app opens, all tasks are loaded again from the saved file.

### 🗑 Delete Tasks
Each task has a delete button, making task management easy.

---

## 📸 Screenshot
*(Upload your GUI screenshot here)*

---

## 📦 How to Run
1. Install Python
2. Download the repository
3. Run the app:
